import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  mainContainer: {
    width: 200,
    height: 'auto',
    marginRight: 14,
  },
  thumbnail: {
    marginBottom: 14,
    width: '100%',
    height: 250,
  },
});
